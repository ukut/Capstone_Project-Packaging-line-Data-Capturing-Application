"""Service layer — business logic.

Services added in Sprint 1+:
    auth_service.py        — password hashing, login validation
    shift_service.py       — shift lifecycle (open, close, lock)
    production_service.py
    review_service.py      — anomaly evaluation, approval workflow
    export_service.py      — legacy Excel export
    audit_service.py       — append-only audit trail
"""
